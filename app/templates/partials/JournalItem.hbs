<div>
  <div>
    <h5>{{body}}</h4>
    <p>{{dateFormat logged_at format="MMMM Do YYYY, h:mm a"}}</p>
  </div>

  <a class="btn btn-link" href="#mentees/{{mentee_id}}/journal_entries/{{id}}">Edit</a>
</div>
<hr>